import Saved from './Saved';

export const metadata = {
  title: 'Saved Coins',
  description: 'A crypto tracker web app by Next.js',
};

const Page = () => {
  return <Saved />;
};

export default Page;
